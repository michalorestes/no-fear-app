<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.stefan.michal.nofear">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/NoFearTheme">
        <activity android:name=".signup.SignUpActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".startscreen.ChallengesActivity" />
        <activity android:name=".signup.SignUpFormActivity" />
        <activity android:name=".startscreen.ViewChallengeActivity" />
        <activity
            android:name=".profile.ProfileActivity"
            android:parentActivityName=".startscreen.ChallengesActivity" />
        <activity
            android:name=".submitchallenge.AddChallengeActivity"
            android:parentActivityName=".startscreen.ChallengesActivity" />
    </application>

</manifest>